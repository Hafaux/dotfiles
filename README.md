# dotfiles

My macOS dotfiles: nvim, yabai, skhd, zsh, git.

![install](.demo/demo.gif)

## Install

```bash
git clone git@github.com:Hafaux/dotfiles.git ~/dotfiles
cd ~/dotfiles
./install.sh
```

Requires [Homebrew](https://brew.sh) (gum is installed automatically)
